<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="time"
            type="String" />

        <variable
            name="name"
            type="String" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/mainConstraint"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/home_background_gradient"
        tools:context=".presentation.home.HomeFragment">

        <include
            android:id="@+id/heading"
            layout="@layout/toolbar" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/standard_margin_start"
            android:layout_marginTop="@dimen/margin_large"
            android:theme="@style/ThemeOverlay.MaterialComponents.Light"
            app:cardCornerRadius="@dimen/margin_normal"
            app:cardElevation="@dimen/_2sdp"
            app:layout_constraintTop_toBottomOf="@id/heading"
            app:strokeWidth="@dimen/_1sdp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/margin_large">

                <TextView
                    android:id="@+id/nameTv"
                    style="@style/TextViewPrimaryBodyNeutral"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginHorizontal="@dimen/margin_large"
                    android:layout_marginTop="@dimen/margin_large"
                    android:paddingBottom="@dimen/margin_small"
                    android:text="@{String.format(@string/hello, name)}"
                    android:textColor="@color/textSecondary"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <TextView
                    android:id="@+id/timeRnTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/hurmeregular"
                    android:text="The time right now is:"

                    android:textColor="@color/textSecondary"
                    android:textSize="@dimen/medium_text"
                    app:layout_constraintStart_toStartOf="@id/nameTv"
                    app:layout_constraintTop_toBottomOf="@id/nameTv" />

                <TextView
                    android:id="@+id/timeTv"
                    style="@style/TextViewSecondaryBody"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/margin_medium"
                    android:text="@{time}"
                    android:textColor="@color/textSecondary"
                    app:layout_constraintStart_toStartOf="@id/timeRnTv"
                    app:layout_constraintTop_toBottomOf="@id/timeRnTv" />

                <androidx.cardview.widget.CardView
                    android:id="@+id/logoutBtn"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="@dimen/margin_large"
                    android:paddingHorizontal="@dimen/margin_large"
                    android:theme="@style/ThemeOverlay.MaterialComponents.Light"
                    app:cardCornerRadius="@dimen/margin_medium"
                    app:cardElevation="@dimen/_2sdp"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    android:clickable="true"
                    android:focusable="true"
                    app:strokeWidth="@dimen/_1sdp">

                    <ImageView
                        android:id="@+id/logoutImg"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:clickable="true"
                        android:focusable="true"
                        android:padding="@dimen/margin_medium"
                        android:src="@drawable/ic_baseline_login_24" />

                </androidx.cardview.widget.CardView>

            </androidx.constraintlayout.widget.ConstraintLayout>

        </com.google.android.material.card.MaterialCardView>

        <ProgressBar
            android:id="@+id/progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/cardview" />

        <TextView
            android:id="@+id/emptyState"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/could_not_load_data"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/cardview" />


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/margin_normal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:clipToPadding="true"
            android:paddingBottom="@dimen/margin_large"
            app:layout_constraintTop_toBottomOf="@id/cardview"
            tools:listitem="@layout/item_medicines" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>